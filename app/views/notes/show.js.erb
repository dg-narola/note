$("#edit-form").html("<%= escape_javascript(render 'show', note: @note, comments: @comments, comment: @comment )%>");
$("#index-form").modal("hide");
$("#show-form").modal("hide");
$("#tag-form").modal("hide")
